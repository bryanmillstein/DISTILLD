<%= flash[:errors].join('<br>').html_safe if flash[:errors] %>
<br>
<br>

<% if logged_in? %>
<p class="greeting">Hello <%= current_user.user_name %></p>
<form action="<%= session_url %>" method="post">
  <%= auth_form %>
    <input type="hidden" name="_method" value="delete">
  <button>Sign Out</button>
</form>
<% end %>
